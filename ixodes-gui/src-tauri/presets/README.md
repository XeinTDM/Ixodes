Preset icons live directly in this directory.

Each preset should include one of the supported filenames:
- Windows: <preset>.ico (preferred) or <preset>.png
- macOS/Linux: <preset>.png

Example:
presets/chrome.ico
presets/chrome.png
